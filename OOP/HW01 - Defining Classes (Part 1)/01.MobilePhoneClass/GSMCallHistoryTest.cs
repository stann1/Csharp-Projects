using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace MobilePhone
{
    class GSMCallHistoryTest
    {
        //static void Main()           //task 12, uncomment to test, or comment in order to use GSMTest
        //{
        //    GSM newPhone = new GSM("Samsung", "Galaxy2", "Pesho", 799.90);

        //    newPhone.AddCall("0888123456", 156);
        //    newPhone.AddCall("0899121212", 34);
        //    newPhone.AddCall("0878789789", 193);

        //    foreach (var call in newPhone.callHistory)          //displays call information
        //    {
        //        Console.WriteLine("{0} called at: {1} (duration: {2} sec)", call.PhoneNumber, call.CallDateTime, call.Duration);
        //    }

        //    newPhone.CalculateBill();         //calculates and prints the bill

        //    newPhone.RemoveCallAtMaxDuration();        //removes longest call and prints new bill
        //    newPhone.CalculateBill();

        //    newPhone.ClearCallHistory();                 //clear and print history
        //    foreach (var call in newPhone.callHistory)
        //    {
        //        Console.WriteLine(call);
        //    }
            
        //}
    }
}
